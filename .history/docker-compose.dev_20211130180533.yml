version: "3.3"

services:
  app:
    container_name: app-dev
    image: app-dev
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    expose:
      - "3000"
    environment:
      VIRTUAL_HOST: admin.projects.sbs
      LETSENCRYPT_HOST: admin.projects.sbs
      LETSENCRYPT_EMAIL: tundeajibawo@gmail.com